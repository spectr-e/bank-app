import styles from '../styles/style.js'
import { clients } from '../constants'

const Clients = () => (
  <section className={`${styles.flexCenter} my-4`}>
    <div className={`${styles.flexCenter} w-full flex-wrap`}></div>
  </section>
)

export default Clients
