import { close, logo, menu } from '../assets'
import { navLinks } from '../constants'

const NavBar = () => {
  return (
    <nav className='flex items-center justify-between w-full py-6 navbar'></nav>
  )
}

export default NavBar
